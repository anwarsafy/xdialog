name: xdialog
description: A highly customizable animated dialog package for Flutter with dynamic configurations, multiple button support, and smooth animations.
version: 1.0.1
homepage: https://github.com/anwarsafy/xdialog
repository: https://github.com/anwarsafy/xdialog
issue_tracker: https://github.com/anwarsafy/xdialog/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true